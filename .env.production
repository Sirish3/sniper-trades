# Baked into the production bundle by `vite build`. No secrets here — only
# the public Render backend URL that the browser needs to call for the Yahoo
# Finance proxy (the Vite dev-server proxy only works locally).
VITE_YAHOO_BASE_URL=https://sniper-trades.onrender.com/yahoo
