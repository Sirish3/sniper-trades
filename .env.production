# Production environment variables baked into the Vite bundle at build time.
# Yahoo Finance proxy is now handled by functions/yahoo/[[path]].js (Cloudflare Pages Function)
# so no VITE_YAHOO_BASE_URL override is needed here.
